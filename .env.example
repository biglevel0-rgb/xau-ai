# -----------------------------------------------------------------------------
# XAU-AI secrets. Copy to `.env` and fill with REAL values on the server only.
# `.env` is git-ignored. NEVER commit real tokens/keys.
# -----------------------------------------------------------------------------

# Telegram bot (owner-only notifications).
# Get a fresh token from @BotFather (revoke any leaked one first).
TELEGRAM_BOT_TOKEN=replace_me
TELEGRAM_OWNER_CHAT_ID=262846950

# Market data providers (fill only those you use).
OANDA_API_TOKEN=
OANDA_ACCOUNT_ID=
TWELVEDATA_API_KEY=

# MetaTrader 5 (local Windows only; leave empty on Linux server).
MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=
